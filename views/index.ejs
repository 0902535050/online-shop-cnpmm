<%- include('partials/header')%>
<header>
    <%- include('partials/navbar')%>
</header>

<div class="slider">
    <div class="load">
    </div>
    <div class="content">
        <div class="principal">
            <h1>TRANSFORMER TOY SHOP</h1>
            <p>hú dè</p>
        </div>
    </div>
</div>

<div class="container mt-auto">

    <form action="/" method="GET" style="margin: 20px;width:30%;">
        <select name="category" class="form-control"> 
            <option value="all">All</option>
            <option value="Thief">Thief</option>
            <option value="Warrior">Warrior</option>
            <option value="Tank">Tank</option>
            <option value="Mage">Mage</option>
            <option value="SoulWaver">Soul Waver</option>
        </select>
        <input type="submit" class="btn btn-primary" value="Filter" style="margin-left:110px;margin-top:5px;width:30%">
    </form>

    <!-- <%
        if(products.length === 0) {%>
            <div class="alert alert-danger">
                There is no products
            </div>

            <%}
            else
            {

            }
    %> -->

    <div class="row">
        <% for(let product of products) { %>

        <!-- <div class="col col-12 col-md-6 col-lg-6 col-xl-3">
            <div class="card" style="width: 18rem;">
                <img src="<%= product.image%>" class="card-img-top">
                <div class="card-body">
                    <h5 class="card-title">
                        <a href="/product/<%=product._id%>"><%=product.name%></a>
                        <p>Price: <%= product.price%></p>
                    </h5>
                    <form action="/cart">
                        <input type="number" name="amount" class="form-control">
                        <input type="submit" name="Add to cart" class="btn btn-primary">
                    </form>
                </div>
            </div>
        </div> -->


        <div class="col-sm-6 col-md-3">
            <div class="thumbnail">
                <img src="<%= product.image%>" alt="..." class="img-responsive" style="max-height: 200px">
                <div class="caption">
                    <h3><a href="/product/<%=product._id%>"><%=product.name%></a></h3>
                    <p class="description"><%=product.description%></p>
                    <div class="clearfix">
                        <div class="price pull-left">
                            <p>Price: $<%= product.price%></p>
                        </div>
                        <form action="/cart">
                            <input type="number" name="amount" class="form-control">
                            <input type="submit" value="Add to cart" class="btn btn-primary"
                                style="margin-top:5px;margin-left: 65px;">
                        </form>
                    </div>
                </div>
            </div>
        </div>
        <% } %>
    </div>
</div>

<%- include('partials/footer')%>